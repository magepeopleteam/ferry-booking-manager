{
  "name": "fbm-booking",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ferry Booking Manager customer booking application.",
  "scripts": {
    "build": "node scripts/check-translations.mjs && vite build && node scripts/write-manifest.mjs",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.29.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
